#include <bits/stdc++.h>
using namespace std;

int main() {
    string str; cin >> str;
    int freq[26] = { 0 };
    for (char ca : str)
        freq[ca - 'a']++;
    for(char i='a'; i<'z'; i++){
        
    }
    return 0;
}